# Diretório de dados
Este diretório armazena o banco de dados SQLite e arquivos temporários.

## Arquivos
- `crawler.db` - Banco de dados principal (gerado automaticamente)
- `export/` - Dados exportados (CSV/JSON)

## Gitignore
Os arquivos .db são ignorados pelo git.
